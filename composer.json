{
    "name": "wordpress-freelance/ws-scheduler",
    "description": "WS Scheduler — système de prise de rendez-vous pour WordPress",
    "type": "wordpress-plugin",
    "license": "GPL-2.0-or-later",
    "require": {
        "php": ">=7.4"
    },
    "require-dev": {
        "phpunit/phpunit": "^9.5",
        "10up/wp_mock": "^0.5.0"
    },
    "scripts": {
        "test": "phpunit --no-coverage",
        "test:coverage": "phpunit"
    },
    "autoload": {
        "psr-4": {
            "WS_Scheduler\\": "includes/"
        }
    }
}
